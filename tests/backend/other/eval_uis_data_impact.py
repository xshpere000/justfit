#!/usr/bin/env python3.14
"""评估 UIS 数据保留策略对分析器的影响"""

import asyncio
from datetime import datetime, timedelta


def print_section(title):
    print("\n" + "=" * 80)
    print(title)
    print("=" * 80)


def assess_impact():
    """评估数据粒度对各分析器的影响"""

    print_section("UIS 数据保留策略评估")

    print("""
UIS 平台数据特点：
  - 小时级数据（cycle=0）：仅保留最近 30 小时（~1.25 天）
  - 天级数据（cycle=1）：保留虚拟机运行期间的所有天数

对比 vCenter：
  - 小时级数据：可保留 30 天的完整小时数据
  - 更细粒度（5分钟级）：可保留 1-7 天
    """)

    print_section("分析器影响评估")

    analyzers = [
        {
            "name": "IdleDetector（闲置VM检测）",
            "data_requirement": "30天CPU/内存使用率",
            "impact": "低",
            "reason": """
✅ 影响有限
- 闲置检测主要看长期（30天）平均使用率
- 天级数据完全足够判断VM是否长期低使用
- 关键指标：avg CPU < 10%, avg Memory < 20%
- 小时级粒度不会改变"闲置"的判断结果
- 示例：30天日均CPU 5% = 闲置，无论是否知道每小时数据
            """.strip()
        },
        {
            "name": "RightSizeAnalyzer（资源配置优化）",
            "data_requirement": "历史峰值、平均值、P95",
            "impact": "中",
            "reason": """
⚠️  有一定影响
- 需要识别CPU/内存的峰值使用情况
- 天级数据会平滑日内峰值：
  * 日均CPU 30% 可能来自：
    - 场景A：全天30%（平稳）
    - 场景B：工作时间60% + 非工作时间10%
- 场景B需要4核，场景A只需要2核
- 影响：推荐配置可能偏保守（over-provision）
- 缓解：仍可基于天级峰值给出合理建议
            """.strip()
        },
        {
            "name": "UsagePatternAnalyzer（使用模式识别）",
            "data_requirement": "峰谷波动、周期性规律",
            "impact": "高",
            "reason": """
❌ 影响较大
- 无法识别日内使用模式：
  * 工作时间vs非工作时间
  * 批处理任务的夜间峰值
  * 周期性短时间 spikes
- 天级数据丢失了日内波动信息
- 影响：
  * 无法识别"潮汐模式"（白天高负载，夜间低负载）
  * 潮汐优化建议可能不准确
  * 无法推荐合适的共享策略
- 示例：
  * 小时数据：9am-6pm CPU 80%，其他时间 10%
  * 天数据：只能看到平均 CPU 40%
  * 无法判断是否适合潮汐共享
            """.strip()
        },
        {
            "name": "MismatchDetector（配置不匹配检测）",
            "data_requirement": "CPU/内存使用率",
            "impact": "低到中",
            "reason": """
⚠️  有一定影响
- 主要检测：配置过大（over-provisioned）
- 天级平均值仍可检测明显的不匹配：
  * 配置8核，日均CPU 10% → 不匹配
  * 配置16GB，日均内存20% → 不匹配
- 影响：可能错过短期峰值导致的不匹配
  * 示例：每天1小时峰值80%，其他时间10%
  * 天级数据看到30%，可能认为配置合理
  * 实际需要更大的配置应对峰值
- 总体：核心功能可用，精度略降
            """.strip()
        },
        {
            "name": "ResourceAnalyzer（资源使用分析）",
            "data_requirement": "总体使用趋势、热力图",
            "impact": "低",
            "reason": """
✅ 影响很小
- 主要分析：整体使用率、分布情况
- 天级数据足够展示趋势：
  * 30天CPU使用率曲线
  * 内存使用热力图
  * 磁盘/IO趋势分析
- 小时级粒度对"宏观分析"影响有限
            """.strip()
        },
        {
            "name": "HealthAnalyzer（平台健康评分）",
            "data_requirement": "当前状态 + 聚合统计",
            "impact": "无",
            "reason": """
✅ 无影响
- 主要基于：
  * 当前资源状态（CPU、内存、存储）
  * 集群/主机数量
  * VM分布情况
- 不依赖历史性能数据的细粒度
- 健康评分完全准确
            """.strip()
        }
    ]

    for i, analyzer in enumerate(analyzers, 1):
        print(f"\n{i}. {analyzer['name']}")
        print(f"   数据需求: {analyzer['data_requirement']}")
        print(f"   影响程度: {analyzer['impact']}")
        print(f"   详细分析:")
        for line in analyzer['reason'].split('\n'):
            print(f"   {line}")

    print_section("实际案例分析")

    print("""
场景1：识别闲置VM（30天数据）
-------------------------------
  vCenter（小时级）：
    - 每小时CPU使用率：5%, 8%, 3%, 6%, ...
    - 30天平均值：5.2%
    - 结论：✅ 闲置VM

  UIS（天级）：
    - 每天平均CPU使用率：5.1%, 5.3%, 4.9%, 5.4%, ...
    - 30天平均值：5.2%
    - 结论：✅ 闲置VM（与vCenter一致）

  影响：无 ✅

场景2：RightSize 推荐（4核VM）
-------------------------------
  vCenter（小时级）：
    - 数据：工作时间9小时CPU 70-80%，其他时间10-20%
    - 峰值：80%
    - 分析：需要应对工作负载
    - 推荐：保持4核或扩容到6核

  UIS（天级）：
    - 数据：每天平均CPU 35-40%
    - 峰值：40%
    - 分析：使用率偏低
    - 推荐：可能建议缩减到2核（错误❌）

  影响：推荐可能不准确 ⚠️

场景3：潮汐模式识别
-------------------------------
  vCenter（小时级）：
    - 模式：9am-6pm CPU 80%，7pm-8am CPU 15%
    - 结论：✅ 潮汐模式（适合共享）
    - 建议：可与批处理VM共享

  UIS（天级）：
    - 数据：每天平均CPU 45%
    - 结论：❌ 看不到潮汐模式
    - 建议：无潮汐优化建议

  影响：完全丢失日内模式信息 ❌
    """)

    print_section("总体评估结论")

    print("""
✅ 可用的分析（影响小或无）：
  1. 闲置VM检测 - 完全可用
  2. 资源使用分析 - 完全可用
  3. 平台健康评分 - 完全可用

⚠️  部分可用的分析（精度略降）：
  4. 配置不匹配检测 - 核心功能可用，但可能错过峰值
  5. 资源配置优化 - 建议可能偏保守

❌ 受限的分析（功能缺失）：
  6. 使用模式识别 - 无法识别日内模式
  7. 潮汐优化 - 无法推荐

建议：
------
1. 对于UIS平台，应明确告知用户数据粒度限制
2. 分析结果中添加"数据粒度说明"：
   - "基于天级数据分析，无法识别日内使用模式"
   - "配置建议基于平均使用率，短期峰值可能被平滑"
3. 考虑为不同平台调整分析策略：
   - UIS：侧重长期趋势分析
   - vCenter：可提供更精细的短期分析
    """)

    print_section("改进建议")

    print("""
方案1：调整分析策略（推荐）
-----------------------------
对UIS平台使用更保守的分析阈值：
  - 闲置检测：avg CPU < 5%（vCenter用10%）
  - RightSize：基于P95而非峰值
  - 明确标注"天级数据"的局限性

方案2：提示用户
-----------------------------
在分析报告中添加说明：
  "⚠️  注意：UIS平台仅提供天级历史数据，
   无法识别日内使用模式。配置建议可能偏保守，
   建议结合实际业务负载调整。"

方案3：利用30小时小时数据
-----------------------------
对于最近30小时，可以提供：
  - 实时监控Dashboard
  - 短期异常检测
  - 与天级数据结合使用
    """)


if __name__ == "__main__":
    assess_impact()
